{
  "name": "opensource-weather-dashboard",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --record false",
    "start:mock": "node scripts/start-mock-backend.js",
    "test": "npm run cy:run"
  },
  "devDependencies": {
    "cypress": "^12.0.0",
    "json-server": "^0.17.0",
    "mochawesome": "^7.0.0",
    "cypress-image-snapshot": "^4.0.1",
    "cypress-axe": "^1.0.0"
  }
}
